all:
  theta:
    host: 100.78.218.73
    ansible_ssh_private_key_file: ~/.ssh/id_ed25519
    specific_host_var: specific_host_var_value
  # alpha:
  #   host: localhost

some_group:
  hosts:
    localhost:
      host: localhost
  vars:
    specific_group_var: specific_group_var_value
