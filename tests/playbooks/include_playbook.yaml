---
- hosts: all
  gather_facts: false
  tasks:
    - name: Initial Task in Main Playbook
      shell: echo "Main playbook start" > /tmp/spage/include_test_main_start.txt

    - include_tasks: included_playbook.yaml

    - name: Final Task in Main Playbook
      shell: echo "Main playbook end" > /tmp/spage/include_test_main_end.txt
